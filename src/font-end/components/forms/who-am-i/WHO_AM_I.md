Purpose: A form that pops up when a user presses their profile.

How it works (in conjunction with other components and pages):
- User clicks profile page
- Form populated below their name and github name (both gathered via the login form on first sign-up)
- Info saved to some free, open source database endpoint (reddis? idk; choose one)
- On save, user profile updated with supplied information

Goal:
- Show HYTE students a simple form that saves user info **and is editable** (login page is not post-submission editable)
